'use strict';

var _startsWith = require('../startsWith');

var encapsulated_anonymus;

encapsulated_anonymus = function encapsulated_anonymus() {
  (0, _startsWith.startsWithjs)('foobar', 'foo');
};
