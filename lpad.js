import { pad as pad_pad } from "./pad";

var mod_anonymus = function lpad(str, length, padStr) {
  return pad_pad(str, length, padStr);
};

mod_anonymus = function lpad(str, length, padStr) {
  return pad_pad(str, length, padStr);
};
export { mod_anonymus as lpad };
