import { chars as chars_chars } from "./chars";

var mod_anonymus = function reverse(str) {
  return chars_chars(str).reverse().join('');
};

mod_anonymus = function reverse(str) {
  return chars_chars(str).reverse().join('');
};
export { mod_anonymus as reverse };
